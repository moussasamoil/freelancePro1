<div class="modal rtl modal-dialog-centered" id="reasonModal" data-bs-keyboard="false" data-bs-backdrop="static" tabindex="-1" aria-labelledby="reasonModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content background-white-green" style="position: relative;">
            <div class="modal-header">
                <button type="button" class="btn head-16 text-green" data-bs-dismiss="modal" aria-label="Close"> تراجع</button>
                <h5 class="modal-title head" id="reasonModalLabel">سبب الفشل</h5>
                <button type="button" class="btn head-16 text-green" onclick="submitStatusChange()"> تأكيد</button>
            </div>
            <div class="modal-body">
                <select id="statusChangeReason" class="form-control drop-down-design filter-btn">
                    <!-- Options populated by fetchFailureReasons() -->
                </select>
            </div>
        </div>
    </div>
</div>

<style>
    .head {
        font-size: 18px;
        text-decoration: none;
        color: black;
    }

    .head-16 {
        font-size: 16px;
        text-decoration: none;
        color: grey;
    }
</style>
